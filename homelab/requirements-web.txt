# Listener homelab — web dashboard + ingest (FastAPI + HTMX). Light venv, no ML deps.
#   uv venv ~/listener-web --python 3.10
#   uv pip install --python ~/listener-web/bin/python -r requirements-web.txt
fastapi
uvicorn[standard]
jinja2
python-multipart
httpx
dateparser   # H4: resolve relative due times → UTC (ADR-017)
mcp          # page assistant: MCP server + client (ADR-020)
apscheduler  # H5: nightly email brief scheduler (ADR-024)
# Google Calendar + Tasks push (ADR-026) — OAuth, NOT the SMTP app password
google-api-python-client
google-auth-oauthlib
google-auth-httplib2
